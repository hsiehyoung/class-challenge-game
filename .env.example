# Neon PostgreSQL 連線字串（未設定時排行榜改用記憶體儲存，重啟即清空，僅適合本機測試）
# DATABASE_URL=postgresql://user:password@ep-xxxx.aws.neon.tech/neondb?sslmode=require

# 允許的前端來源，逗號分隔（未設定時允許所有來源，僅建議本機開發使用）
# ALLOWED_ORIGIN=https://<你的帳號>.github.io

# PORT=3000
